spring:
  application:
    name: lab-3-client
  cloud:
    config:
      uri: http://localhost:8001
  profiles:
    active: northamerica
server:
  port: 8002
#  Note: you could also run the application with Dspring.profiles.active=northamerica instead of setting spring.profiles.active=northamerica